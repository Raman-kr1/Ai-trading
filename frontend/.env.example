# Override the proxied backend if running it on a non-default host/port.
# In production, set VITE_API_BASE to the absolute backend URL.
VITE_BACKEND_URL=http://localhost:3000
VITE_API_BASE=
VITE_WS_URL=
